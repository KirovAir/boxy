@{ Layout = null; }
@* Shared shell for every email. Inline styles only - email clients strip <style>/external CSS. *@
<div style="font-family:system-ui,-apple-system,Segoe UI,Roboto,Arial,sans-serif;color:#2b2118;max-width:520px;">
    @RenderBody()
</div>
